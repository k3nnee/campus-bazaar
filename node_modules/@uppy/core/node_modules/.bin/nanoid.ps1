#!/usr/bin/env pwsh
$basedir=Split-Path $MyInvocation.MyCommand.Definition -Parent

$exe=""
if ($PSVersionTable.PSVersion -lt "6.0" -or $IsWindows) {
  # Fix case when both the Windows and Linux builds of Node
  # are installed in the same directory
  $exe=".exe"
}
$ret=0
if (Test-Path "$basedir/node$exe") {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:node_modules/.bin/uuid.ps1
    $input | & "$basedir/node$exe"  "$basedir/../uuid/dist/bin/uuid" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../uuid/dist/bin/uuid" $args
========
    $input | & "$basedir/node$exe"  "$basedir/../nanoid/bin/nanoid.js" $args
  } else {
    & "$basedir/node$exe"  "$basedir/../nanoid/bin/nanoid.js" $args
>>>>>>>> origin/dev:node_modules/@uppy/core/node_modules/.bin/nanoid.ps1
  }
  $ret=$LASTEXITCODE
} else {
  # Support pipeline input
  if ($MyInvocation.ExpectingInput) {
<<<<<<<< HEAD:node_modules/.bin/uuid.ps1
    $input | & "node$exe"  "$basedir/../uuid/dist/bin/uuid" $args
  } else {
    & "node$exe"  "$basedir/../uuid/dist/bin/uuid" $args
========
    $input | & "node$exe"  "$basedir/../nanoid/bin/nanoid.js" $args
  } else {
    & "node$exe"  "$basedir/../nanoid/bin/nanoid.js" $args
>>>>>>>> origin/dev:node_modules/@uppy/core/node_modules/.bin/nanoid.ps1
  }
  $ret=$LASTEXITCODE
}
exit $ret
